# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-O2 -pipe"
TUNE="-march=native -mtune=native"
PYTHON_TARGETS="python3_9"

CFLAGS="${COMMON_FLAGS} ${TUNE}"
CXXFLAGS="${COMMON_FLAGS} ${TUNE}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"
#MAKEOPTS="-j3"
ACCEPT_LICENSE="-* @FREE quake1-demodata lha unRAR microsoft-edge"
# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
INPUT_DEVICES="libinput evdev synaptics"
LC_MESSAGES=C
RUBY_TARGETS=ruby27
VIDEO_CARDS="intel i915"
L10N=""
USE="X ncurses evdev jpeg gpm vim-syntax mplayer alsa png introspection gnutls cairo tk -acl -blksha1 -gstreamer -bidi -elogind -unwind -geolocation -orc -ruby -perl -sex -vala -php -ruby -branding -accessability -kde -gnome -pulseaudio -emacs -atk -rust -gles2-only -static opengl dbus -qt -qt5 gtk jit"
